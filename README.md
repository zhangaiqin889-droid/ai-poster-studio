# PosterForge — 静态演示站点

这是 AI Poster Design Studio 的公开静态演示站点（仅编译产物，无源码、无密钥、无后端）。

- 通过 GitHub Pages 托管。
- 前端以 `VITE_USE_MOCK=true` 构建，纯前端内存 mock 数据，不连接任何真实后端或数据库。
- 主项目源码在私有仓库 `AI-Agent-System` 中维护；本仓库只做公开访问用途。
